## Crypto Websockets 
This example tries to showcase how glee could be used as a websocket client. There is a server that streams the price change of a fake crypto currency (we can call it asyncapicoin) and the client subscribes to this stream and draws a graph in the terminal. 

<img src="./graph.png">